name: Build Flet Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y curl git unzip xz-utils zip libglu1-mesa
        pip install --upgrade pip
        pip install flet requests pillow qrcode openpyxl

    - name: Setup Java JDK
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Build APK using Flet
      run: |
        # নো-ভার্সন চেক দিয়ে নিখুঁতভাবে বিল্ড করার জন্য
        flet build apk --release --no-version-check --suppress-analytics

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Attendance-App-APK
        path: build/apk/*.apk
